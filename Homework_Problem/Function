Functions Question No.1 :-

[ calculating the area of a rectangle using function] 

# Outcome :- The area of the rectangle is 15 square units.


def calculate_rectangle_area(length, width):
    """
    Calculate the area of a rectangle given its length and width.
    
    Args:
        length (float): The length of the rectangle.
        width (float): The width of the rectangle.
    
    Returns:
        float: The area of the rectangle.
    """
    return length * width  

length = 5
width = 3
area = calculate_rectangle_area(length, width)
print(f"The area of the rectangle is {area} square units.")

Question 2:- [ "Functions" ]

[ function to determine whether loan should be approved based on income, credit score and loan amount ]

# The outcome of these below code is - [ "True" Statement] Loan approval.

def approve_loan(income, credit_score, loan_amount):
    """
    Determine whether a loan should be approved based on income, credit score, and loan amount.
    
    Args:
        income (float): The borrower's income.
        credit_score (int): The borrower's credit score.
        loan_amount (float): The loan amount.
    
    Returns:
        bool: Whether the loan should be approved.
    """
    if income > 50000 and credit_score > 700 and loan_amount < 100000:
        return True
    else:
        return False
        
income = 60000
credit_score = 750
loan_amount = 80000
approval = approve_loan(income, credit_score, loan_amount)
print(f"Loan approval: {approval}")


